# 컨텍스트 노트 — 의사결정 기록

작업: Nemotron 한국 페르소나가 재난안전 인식 설문에 1인칭 응답 → 합성 설문 데이터 생성·분석.

## 핵심 결정

### D1. 페르소나당 1회 호출, JSON 한 덩어리로 전체 응답
- 이유: 문항별 호출은 비용·지연이 30배. 순위형·매트릭스가 많아 구조화 출력(JSON)이 파싱에 유리.
- 위험: 한 번에 30문항 → 모델이 일부 누락 가능. → 응답 검증 후 누락 시 재시도.

### D2. Q1~Q4(일반현황)는 LLM에 묻지 않고 페르소나 데이터에서 직접 매핑
- 이유: 인구통계는 데이터에 이미 있음. LLM에 물으면 환각·불일치 발생. 직접 매핑이 정확하고 토큰 절약.
- 매핑:
  - Q1 성별: 남자→1, 여자→2
  - Q2 연령: age 그대로 (데이터 전부 19~99세)
  - Q3 학력: {무학,초등학교,중학교,고등학교}→1(고졸이하), {2~3년제 전문대학,4년제 대학교}→2(대졸), {대학원}→3
  - Q4 지역(데이터명→설문코드): 서울1 부산2 대구3 인천4 광주5 대전6 울산7 세종8 경기9 강원10 충청북11 충청남12 전북13 전라남14 경상북15 경상남16 제주17
  - 주의: 데이터 지역명이 일부 약어/전체 혼용 (전북 vs 전라남). 정확한 문자열로 매핑.

### D3. 층화추출 (표 7.3 표본할당 재현) — 변경됨
- 참조: survey_ref/대국민_표본추출.pdf
- 모집단: 만 **19~69세** (Nemotron 19~99 중 필터). 17개 시도.
- 층화변수: 시도(17)×성별(2)×연령대(5: 19-29/30-39/40-49/50-59/60-69) = 170셀.
- 배분: 문서 표 7.3 할당표(합 1000, 제곱근+비례배분 결과)를 목표 N으로 비례확대(최대잔여법). N=2000 → ×2.
- 층 내 랜덤(seed=42). 검증: 34개 (지역×성별) 셀 전부 표7.3×2와 일치 확인.
- 풀: 19~69세·17시도 = 848,296명. 부족 셀 없음.
- 주의: 학력은 층화변수 아님 → Nemotron 자연분포(고졸이하49/대졸46/대학원6%). 참조설문(대졸72/대학원17%, 온라인패널 고학력편중)과 다름. 우리 쪽이 실제 인구분포에 근접.
- 사후가중치(표 7.2 모집단 기반): 표본이 제곱근배분이라 모집단 추정 시 가중 필요 → **분석 단계에서 적용 예정**.

### D4. 모델 확정: zai-glm-4.7 (파일럿 결과)
- Cerebras 실제 제공 모델: `zai-glm-4.7`, `gpt-oss-120b` (둘 다 thinking 추론모델).
- 파일럿(8명) 결과: GLM-4.7이 블루칼라 직업의 산업재해 위험, 가족 디테일 등 **페르소나 반영도 우세**. gpt-oss는 최빈값 수렴(분화 약함).
- 추론모델 주의: reasoning는 message.reasoning(별도), 최종답은 message.content. max_tokens 넉넉히(4000) 필요. content 비면 재시도.
- gpt-oss는 reasoning_effort(low/med/high) 지원, GLM은 thinking 끄기 미지원.
- 비용/속도: 8명/68초(workers4). 본 실행은 동시성 상향.

### D5. Cerebras 호출은 OpenAI SDK + base_url 사용
- `base_url="https://api.cerebras.ai/v1"`, `api_key=os.environ["CEREBRAS_API_KEY"]`.
- cerebras-cloud-sdk 미설치, openai 2.37 설치돼 있어 호환 경로 사용.

## 설문 문항 유형 요약 (응답 스키마)
- 5점척도: Q5,Q6,Q14,Q16,Q20,Q28 / 6번 잘모름 포함: Q21
- 단일선택: Q7(12),Q13(5),Q22(5),Q27(5),Q29(예/아니오),Q29-1,Q29-2
- 순위형(N개 정렬): Q8/Q9/Q10/Q11/Q23/Q24/Q25/Q26(3개), Q12(2개), Q15(2개)
- 매트릭스(3항목×5점): Q17,Q18,Q19
- 주관식: Q30
- 재난유형 마스터 1~70은 Q8~Q11, Q23~Q26 공용.

## 환경
- Python 3.14.4 (`py` 런처 사용. `python`은 WindowsApps 스텁이라 작동X)
- 설치됨: datasets 4.8.5, pyarrow 24, pandas 3.0.2, openai 2.37, tqdm 4.67
- 데이터: `data/data/train-0000X-of-00009.parquet` (파일당 111,112행)
- 콘솔 한글: `chcp 65001` + `$env:PYTHONUTF8=1` 필요.

### D6. 처리 속도 = TPM 병목 (확정, 재탐색 불필요)
- Cerebras 계정의 **분당 토큰(TPM) 한도**가 진짜 병목. GLM-4.7은 호출당 추론토큰 ~2000~3000.
- 동시성 상향은 처리율 불변 + 429(TPM/RPM) 실패만 증가. (workers16/2.5s=56성공·24실패 vs workers8/4.0s=74성공·0실패, 동일 시간.)
- **최적 설정: workers 8 + --min-interval 4.0s** (실패 0, ~0.067건/초). 2000명 ≈ 8시간.
- 사용자 결정: 느려도 GLM-4.7 유지, gpt-oss 미사용. 단축하려면 Cerebras 티어 상향 필요.

## 확정 사항 (추가)
- 본 실행 샘플 크기: **2000명** (seed=42, 층화추출)
- **1차 범위: Q1~Q13만** (Q1~Q4 매핑 + Q5~Q13 LLM 9문항). Q14~Q30은 2차.
  - run_survey.py --phase phase1 / build_prompt·validate가 문항 부분집합 지원.
- 파일럿: 8명 × 2모델 비교 후 모델 확정

## 미해결/확인 필요
- CEREBRAS_API_KEY 설정 (현재 미설정 → 사용자 설정 대기)
- 파일럿 결과로 zai-glm-4.6 vs gpt-oss-120b 확정
